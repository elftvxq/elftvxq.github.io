<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Leetcode No.696 Count Binary Substrings 📒</title>
    <url>/2020/04/17/Leetcode%20No.696%20Count%20Binary%20Substrings/</url>
    <content><![CDATA[<blockquote>
<p>題號：No.696</p>
<p>類型：字串 String</p>
<p>難度：簡單</p>
<p>使用語言：JavaScript</p>
</blockquote>
<a id="more"></a>
<h3 id="題目"><a class="header-anchor" href="#題目">¶</a>題目</h3>
<p>Give a string s, count the number of non-empty (contiguous) substrings that have the same number of 0’s and 1’s, and all the 0’s and all the 1’s in these substrings are grouped consecutively.</p>
<p>Substrings that occur multiple times are counted the number of times they occur.</p>
<h5 id="Example-1"><a class="header-anchor" href="#Example-1">¶</a>Example 1:</h5>
<blockquote>
<p>Input: “00110011”<br>
Output: 6<br>
Explanation: There are 6 substrings that have equal number of consecutive 1’s and 0’s: “0011”, “01”, “1100”, “10”, “0011”, and “01”.</p>
</blockquote>
<blockquote>
<p>Notice that some of these substrings repeat and are counted the number of times they occur.</p>
</blockquote>
<blockquote>
<p>Also, “00110011” is not a valid substring because all the 0’s (and 1’s) are not grouped together.</p>
</blockquote>
<h5 id="Example-2"><a class="header-anchor" href="#Example-2">¶</a>Example 2:</h5>
<blockquote>
<p>Input: “10101”<br>
Output: 4<br>
Explanation: There are 4 substrings: “10”, “01”, “10”, “01” that have equal number of consecutive 1’s and 0’s.</p>
</blockquote>
<h6 id="Note"><a class="header-anchor" href="#Note">¶</a>Note:</h6>
<ul>
<li>s.length will be between 1 and 50,000.</li>
<li>s will only consist of “0” or “1” characters.</li>
</ul>
<hr>
<h3 id="解題切入的知識點"><a class="header-anchor" href="#解題切入的知識點">¶</a>解題切入的知識點</h3>
<ul>
<li>string.prototype.sort</li>
<li>正規表達式 /g
<ul>
<li>對於表達式對象的 exec 方法，不加入 g，則只返回第一個匹配，無論執行多少次均是如此，如果加入 g，則第一次執行也返回第一個匹配，再執行返回第二個匹配，依次類推。</li>
</ul>
</li>
<li>數組常用操作方法: <a href="https://juejin.im/post/59f848026fb9a0450167420a" target="_blank" rel="noopener">https://juejin.im/post/59f848026fb9a0450167420a</a></li>
</ul>
<hr>
<ol>
<li>使用頭部或尾部的陣列刪除方法
<ul>
<li>數組尾部删除 <em>pop（）</em>：方法用於删除並且返回陣列最後一個元素。用法：<code>arrayObject.pop()</code></li>
</ul>
</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">push()、pop()、unshift()、shift()</span><br></pre></td></tr></table></figure>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">btn[<span class="number">3</span>].onclick =</span><br><span class="line"><span class="function"><span class="keyword">function</span></span></span><br><span class="line"><span class="function">(<span class="params"></span>)</span>&#123;	<span class="keyword">var</span> arr = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>]	</span><br><span class="line">arr.pop()	</span><br><span class="line">alert(arr) <span class="comment">//1,2,3,4</span></span><br><span class="line">&#125;<span class="comment">//尾部删除一個</span></span><br></pre></td></tr></table></figure>
<ol start="2">
<li>數組頭部添加 <em>unshift()</em> 方法可向數组的開頭增加一個或更多元素，並返回新的長度。語法：<code>JavaScript  arrayObject.unshift(newelement1,newelement2,....,newelementX)</code></li>
</ol>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">btn[<span class="number">5</span>].onclick =</span><br><span class="line"><span class="function"><span class="keyword">function</span></span></span><br><span class="line"><span class="function">(<span class="params"></span>)</span>&#123;	<span class="keyword">var</span> arr = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>]	</span><br><span class="line">arr.shift()	</span><br><span class="line">alert(arr)<span class="comment">//2,3,4,5</span></span><br><span class="line">&#125;<span class="comment">//頭部删除一個</span></span><br></pre></td></tr></table></figure>
<h3 id="解法"><a class="header-anchor" href="#解法">¶</a>解法</h3>
<p>思考邏輯：</p>
<ol>
<li>先將數字對應的英文字母列出</li>
<li>將數字分割，EX: 234 ⇒ [2,3,4]</li>
<li>組出一個新的陣列，也就是數字所對應英文字母的陣列</li>
<li>先讓23兩個數字進行組合，再跟下一個數字進行組合</li>
</ol>
<h3 id="font-color-FF0000-注意點-font"><a class="header-anchor" href="#font-color-FF0000-注意點-font">¶</a><font color=#FF0000>注意點</font></h3>
<ol>
<li>在把數字對應的字母塞回陣列的時候，要記得判斷如果該數字有字母再塞回去</li>
<li>跑 for 迴圈長度想得太困難，！arr[0] 代表的意思是說，比如先拿 2,3 兩兩去循環組合，2 = ‘abc’, 所以循環的長度就是 3</li>
<li>要把一剛開始的 2,3 數字替換掉</li>
<li>記得返回取得的數組</li>
<li>最後面要返回 function</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">export</span> <span class="keyword">default</span> (str) =&gt; &#123;</span><br><span class="line">        <span class="comment">//建立數字對應字母</span></span><br><span class="line">        <span class="keyword">let</span> map =[<span class="string">''</span>,<span class="number">1</span>, <span class="string">'abc'</span>, <span class="string">'def'</span>, <span class="string">'ghi'</span>, <span class="string">'jkl'</span>, <span class="string">'mno'</span>, <span class="string">'pqrs'</span>, <span class="string">'tux'</span>, <span class="string">'wxyz'</span>]</span><br><span class="line">        <span class="comment">//把輸入字串按照單字分隔，234=&gt;2,3,4</span></span><br><span class="line">        <span class="keyword">let</span> num = str.split(<span class="string">''</span>);</span><br><span class="line">        <span class="comment">// 保存鍵盤映射的字母內容，23=&gt;[abc, def]</span></span><br><span class="line">        <span class="keyword">let</span> code = []</span><br><span class="line">        num.forEach(<span class="function"><span class="params">item</span> =&gt;</span> &#123;</span><br><span class="line">            <span class="keyword">if</span>(map[item])&#123;</span><br><span class="line">                code.push(map[item])</span><br><span class="line">            &#125;       </span><br><span class="line">        &#125;);</span><br><span class="line">        <span class="built_in">console</span>.log(code);</span><br><span class="line">    </span><br><span class="line">        <span class="keyword">let</span> comb = <span class="function">(<span class="params">arr</span>) =&gt;</span> &#123;</span><br><span class="line">            <span class="comment">//臨時變量，用來保存前兩個組合的結果</span></span><br><span class="line">           <span class="keyword">let</span> temp = []; </span><br><span class="line">           <span class="comment">// 最外層的循環式是跑遍第一個元素，第二個循環是跑遍第二個元素</span></span><br><span class="line">           <span class="keyword">let</span> il = arr[<span class="number">0</span>].length;</span><br><span class="line">           <span class="keyword">let</span> jl = arr[<span class="number">1</span>].length; </span><br><span class="line">           <span class="keyword">for</span> (<span class="keyword">let</span> i = <span class="number">0</span>; i &lt; il ; i++) &#123;     </span><br><span class="line">               <span class="keyword">for</span> (<span class="keyword">let</span> j = <span class="number">0</span>; j &lt; jl; j++) &#123;</span><br><span class="line">                   temp.push(<span class="string">`<span class="subst">$&#123;arr[<span class="number">0</span>][i]&#125;</span><span class="subst">$&#123;arr[<span class="number">1</span>][j]&#125;</span>`</span>)</span><br><span class="line">               &#125;</span><br><span class="line">           &#125; </span><br><span class="line">    				<span class="comment">//替換掉一開始組合的2,3，準備跟下一個繼續尬for迴圈</span></span><br><span class="line">           arr.splice(<span class="number">0</span>, <span class="number">2</span>, temp);</span><br><span class="line">    				<span class="comment">//如果還有長度繼續尬</span></span><br><span class="line">           <span class="keyword">if</span>(arr.length &gt; <span class="number">1</span> )&#123;</span><br><span class="line">               comb(arr)</span><br><span class="line">           &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    				<span class="comment">//沒有就把陣列返回就好</span></span><br><span class="line">               <span class="keyword">return</span> temp;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="comment">//最後是要返回陣列的第一個組合</span></span><br><span class="line">           <span class="keyword">return</span> arr[<span class="number">0</span>];</span><br><span class="line">             </span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> comb(code);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>leetcode 刷題</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
        <tag>leetcode</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 觀念測試 | 01</title>
    <url>/2020/08/30/JavaScript%E6%AF%8F%E6%97%A5%E8%80%83%E9%A1%8C/</url>
    <content><![CDATA[<blockquote>
<p>前言：<br>
先前讀《超速學習》，其中有一個學習方式是直接透過考題驗證自己學習的東西是不是正確，並且要有即時的反饋才能修正錯誤。在讀了一些 JavaScript 的觀念後，也想驗證自己是否真的理解且吸收，因此在網路上找了一些考題，把比較有趣的記錄下來，並且寫下解題思路。</p>
</blockquote>
<h4 id="1-以下程式碼片段，請問-a-length-的值是多少？"><a class="header-anchor" href="#1-以下程式碼片段，請問-a-length-的值是多少？">¶</a>1. 以下程式碼片段，請問 a.length 的值是多少？</h4>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a=[]; </span><br><span class="line">a[<span class="number">0</span>]=<span class="number">1</span>; </span><br><span class="line">a[<span class="number">1</span>]=<span class="number">2</span>; </span><br><span class="line">a[<span class="number">2</span>]=<span class="number">3</span>; </span><br><span class="line">a[<span class="number">5</span>]=<span class="number">4</span>;</span><br></pre></td></tr></table></figure>
<p>選項：<br>
A. 會報錯<br>
B. 3<br>
C. 4<br>
D. 6</p>
<h5 id="答案-6"><a class="header-anchor" href="#答案-6">¶</a>答案: 6</h5>
<p>解析：本來以為答案是會報錯，原來沒有賦值的元素會自動給 undefined。所以陣列 a 會是這樣：</p>
<blockquote>
<p>Array[6]<br>
0: 1<br>
1: 2<br>
2: 3<br>
3:undefined<br>
4:undefined<br>
5: 4<br>
length: 6</p>
</blockquote>
<h4 id="2-請問在-JavaScript-程式中，alert-undefined-null-的輸出結果是？"><a class="header-anchor" href="#2-請問在-JavaScript-程式中，alert-undefined-null-的輸出結果是？">¶</a>2. 請問在 JavaScript 程式中，alert(undefined==null)的輸出結果是？</h4>
<p>選項：<br>
A. null<br>
B. undefiend<br>
C. true<br>
D. false<br>
E. 以上答案皆不正確</p>
<h5 id="答案：true"><a class="header-anchor" href="#答案：true">¶</a>答案：true</h5>
<p>解析：<br>
這題問法有些為不嚴謹，第一個想法是在兩個等號比較值的狀況下，會想先把 undefined 和 null 轉成數字，undefiened 是 NaN，null 則為 0，所以一開始選了 fasle。後來查了資料發現，兩個運算符比較這兩個形態會是true，undefined 和 null 如果被放在 if 當中，都會被視作為 false，是比較特殊的地方。</p>
<p>如果是use strict 的條件下，會是 false。</p>
<p>額外補充：<br>
== 比較兩者的值，=== 較為嚴謹還需比較型態。如果是三個等號的問法，undefined 類型為 undefined，null 類型為 object。</p>
<h4 id="3-請問-a-的結果是什麼？"><a class="header-anchor" href="#3-請問-a-的結果是什麼？">¶</a>3. 請問 a 的結果是什麼？</h4>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span>(! <span class="string">"a"</span> <span class="keyword">in</span> <span class="built_in">window</span>)&#123;</span><br><span class="line">    <span class="keyword">var</span> a = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line">alert(a);</span><br></pre></td></tr></table></figure>
<p>選項：<br>
A. null<br>
B. 1<br>
C. undefiend<br>
D. 以上答案皆不正確</p>
<h5 id="答案-C"><a class="header-anchor" href="#答案-C">¶</a>答案: C</h5>
<p>解析：<br>
先解讀函式，假如變數 a 沒有在 window，就不會進入到條件式中。如果有 a ，a 則等於 1。</p>
<p>var 宣告變數會被 hoisting，所以這段程式碼應該會是以下這樣：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> a</span><br><span class="line"><span class="keyword">if</span>(! <span class="string">"a"</span> <span class="keyword">in</span> <span class="built_in">window</span>)&#123;</span><br><span class="line">    a = <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line">alert(a);</span><br></pre></td></tr></table></figure>
<p>a 其實是全域變數，不會進入到條件式中，alert(a)但是 a 尚未被賦值，答案是 undefined。</p>
<h4 id="4-請給出這段程式碼的執行結果"><a class="header-anchor" href="#4-請給出這段程式碼的執行結果">¶</a>4. 請給出這段程式碼的執行結果?</h4>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> bb = <span class="number">1</span>;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">aa</span>(<span class="params">bb</span>) </span>&#123;</span><br><span class="line">    bb = <span class="number">2</span>;</span><br><span class="line">    alert(bb);</span><br><span class="line">&#125;;</span><br><span class="line">aa(bb);</span><br><span class="line">alert(bb);</span><br></pre></td></tr></table></figure>
<p>選項：<br>
A. 11<br>
B. 12<br>
C. 21<br>
D. 22</p>
<h5 id="答案：C"><a class="header-anchor" href="#答案：C">¶</a>答案：Ｃ</h5>
<p>解析：<br>
alert(bb)取全域變數 bb 的值為 1 沒有問題，比較需要思考的是 function aa 裡的 bb。bb 是參數，不是被定義為全局面量，所以並不會覆蓋到 第一行 var bb = 1。</p>
<p>其實是像這樣：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">var</span> bb =<span class="number">1</span>;</span><br><span class="line"> </span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">aa</span>(<span class="params">bb</span>) </span>&#123;<span class="comment">// 這裏隱藏了一個宣告變量， var bb；</span></span><br><span class="line"> </span><br><span class="line">   bb = <span class="number">2</span>;<span class="comment">// 對函式作用域進行賦值  bb = 2 ；</span></span><br><span class="line"> </span><br><span class="line">   alert(bb);<span class="comment">//弹出 bb 的值 2；</span></span><br><span class="line"> </span><br><span class="line">&#125;;</span><br><span class="line"> </span><br><span class="line">aa(bb);</span><br><span class="line"> </span><br><span class="line">alert(bb);<span class="comment">// 根據 RHS 查找變數 bb，變數值為 1</span></span><br></pre></td></tr></table></figure>
<p>使用函數的形參形成的私有變量，必須要把這個變量賦值給另外一個變量才可以使用，也就是獲得到形參的值。</p>
]]></content>
      <categories>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>JavaScript練習</tag>
      </tags>
  </entry>
  <entry>
    <title>TypeScript 學習系列 | Array, Tuple 和 interface 使用</title>
    <url>/2020/04/19/TypeScript-%E5%AD%B8%E7%BF%92%E7%B3%BB%E5%88%97-Array-Tuple-%E5%92%8C-interface-%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<h3 id="Array"><a class="header-anchor" href="#Array">¶</a>Array</h3>
<p>可以將陣列的元素強制定義為某一個特定的類型，只要在類型的後面加上陣列的括號 [] 即可。</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 陣列裡面全部要是數字</span></span><br><span class="line"><span class="keyword">let</span> arrOFNumber: <span class="built_in">number</span>[] = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>]</span><br><span class="line">arrOFNumber.push(<span class="number">5</span>)</span><br><span class="line">arrOFNumber.push(<span class="string">'str'</span>) <span class="comment">//會報錯</span></span><br></pre></td></tr></table></figure>
<a id="more"></a>
<h3 id="Tuple"><a class="header-anchor" href="#Tuple">¶</a>Tuple</h3>
<p>Tuple的中文可以翻成「元組」，學過 Python 的人對這個資料結構應該比較熟悉。</p>
<blockquote>
<p>應用狀況：陣列裡包含除了某特定元素外的類型</p>
</blockquote>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> users: [<span class="built_in">string</span>, <span class="built_in">number</span>] = [<span class="string">'emma'</span>, <span class="number">20</span>]</span><br><span class="line">user = [<span class="string">'Andy'</span>, <span class="number">2</span>, <span class="literal">true</span>] <span class="comment">// true為布林值，會報錯</span></span><br></pre></td></tr></table></figure>
<h3 id="Object"><a class="header-anchor" href="#Object">¶</a>Object</h3>
<h4 id="TypeScript-的-Interface"><a class="header-anchor" href="#TypeScript-的-Interface">¶</a>TypeScript 的 Interface:</h4>
<ol>
<li>針對使用對象的型態(shape)進行約束，例如屬性或是方法。</li>
<li>對類(class)進行抽象。</li>
<li>Duck typing：動態類型語言會推測資料的型別，比起關注資料類型，更加在意資料如何被使用。</li>
</ol>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 建議 class 的字首大寫表示為 interface</span></span><br><span class="line"><span class="comment">// 注意使用分號「;」結尾</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">interface</span> Person &#123;</span><br><span class="line">	name: <span class="built_in">string</span>;</span><br><span class="line">	age: <span class="built_in">number</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 不能減少或是增加屬性，否則會報錯</span></span><br><span class="line"><span class="keyword">let</span> emma: Person = &#123;</span><br><span class="line">	name: <span class="string">'emma'</span>,</span><br><span class="line">	age: <span class="number">20</span>,</span><br><span class="line">	gender: <span class="string">'female'</span> <span class="comment">//報錯</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="可選屬性"><a class="header-anchor" href="#可選屬性">¶</a>可選屬性</h4>
<p>如果 interface 只能依照上面方法使用會沒有彈性。因此可選屬性能夠幫忙解決這個問題，有或沒有都不影響。</p>
<p><strong>使用方法：在變數名稱後面加上問號</strong></p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> Person &#123;</span><br><span class="line">	name: <span class="built_in">string</span>;</span><br><span class="line">	age?: <span class="built_in">number</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="唯讀屬性"><a class="header-anchor" href="#唯讀屬性">¶</a>唯讀屬性</h4>
<p>複製 class 的時候才使用，只能用在屬性上，且不能被更改賦予的值。</p>
<p><strong>使用方法：在變數名稱前加上 readonly</strong></p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">interface</span> Person &#123;</span><br><span class="line">	readonly id: <span class="built_in">number</span>;</span><br><span class="line">	name: <span class="built_in">string</span>;</span><br><span class="line">	age?: <span class="built_in">number</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>強制更改值的話會報錯</p>
<p><img src="https://upload.cc/i1/2020/04/19/ZxnP7i.png" alt="example"></p>
]]></content>
      <categories>
        <category>TypeScript學習筆記</category>
      </categories>
      <tags>
        <tag>TypeScript</tag>
        <tag>學習心得</tag>
        <tag>前端</tag>
      </tags>
  </entry>
  <entry>
    <title>TypeScript-學習系列-ENUM</title>
    <url>/2020/08/26/TypeScript-%E5%AD%B8%E7%BF%92%E7%B3%BB%E5%88%97-ENUM/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>TypeScript 學習系列 | 什麼是 TypeScript？ 類型有哪些？</title>
    <url>/2020/04/19/TypeScript-%E5%AD%B8%E7%BF%92%E7%B3%BB%E5%88%97-%E4%BB%80%E9%BA%BC%E6%98%AF-TypeScript%EF%BC%9F-%E9%A1%9E%E5%9E%8B%E6%9C%89%E5%93%AA%E4%BA%9B%EF%BC%9F/</url>
    <content><![CDATA[<p>為什麼要使用 TypeScript 呢？好處在於掌握輸入和輸出的數值以及類型，編寫的時候即時顯示提供錯誤，輸入的參數也可以先定義好，避免踩到不清楚資料型別時的坑，比如資料等於 0 是數字還是布林值這種。</p>
<a id="more"></a>
<h4 id="程式語言類型有以下兩種："><a class="header-anchor" href="#程式語言類型有以下兩種：">¶</a>程式語言類型有以下兩種：</h4>
<blockquote>
<p>動態類型語言：執行期間才會檢查數據，初始化時不用給定型態或變量，ex: JavaScript, Ruby, Python<br>
靜態類型語言：編譯階段即執行形態檢查，須在執行前聲明變量或是數據類型， TypeScript 即屬於這類型。</p>
</blockquote>
<hr>
<p>安裝方法，打開終端機輸入</p>
<pre><code>npm install -g typescript 
</code></pre>
<p>檢查安裝版本</p>
<pre><code>tsc -v
</code></pre>
<p>執行 ts 檔案自動生成 js 檔 （要先移動到該目錄）</p>
<pre><code>tsc hello.ts
</code></pre>
<p>第一個 TypeScript 檔案就從 Hello World 開始吧！<br>
建立檔案的時候，不要忘記檔名是 <em>.ts</em> 結尾。</p>
<p>使用方法不難，在變數後方指定傳入的類型，如果傳入的是數字或是非字串等類型，編輯器就會報錯</p>
<pre><code>const hello = (name: string) =&gt; {
	return `hello ${name}`
}

hello('emma')
</code></pre>
<h2 id="TypeScript-類型"><a class="header-anchor" href="#TypeScript-類型">¶</a>TypeScript 類型</h2>
<h4 id="基礎類型"><a class="header-anchor" href="#基礎類型">¶</a>基礎類型</h4>
<p>要進入 TypeScript 類型之前，複習一下 JavaScript 中七個原始類型(Primitive type):</p>
<ul>
<li>Boolean</li>
<li>Null</li>
<li>Undefined</li>
<li>Number</li>
<li>Biglnt (ES6)</li>
<li>String</li>
<li>Symbol (ES6)</li>
</ul>
<p>為什麼叫做原始值呢？本身的值無法被改變，例如操作字串一定會返回一個新的字串，原始字串並沒有被改變。</p>
<p>TypeScript 定義類型的方法很簡單，如前面提到在變數後面加上冒號和型別類型即可。以下是範例</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> isDone: <span class="built_in">boolean</span> = <span class="literal">false</span></span><br><span class="line"><span class="keyword">let</span> age: <span class="built_in">number</span> = <span class="number">20</span></span><br><span class="line"><span class="keyword">let</span> binaryNumebr: <span class="built_in">number</span> = <span class="number">0b1111</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//  ES6中："0b"開頭表示將二進位轉成十進位</span></span><br><span class="line"><span class="comment">//  其他進位表示：</span></span><br><span class="line"><span class="comment">//  八進位: 010(ES5.1); 0o10(ES6)</span></span><br><span class="line"><span class="comment">//  十六進位: 0x10(ES5.1)</span></span><br><span class="line"><span class="comment">//  二進位: 0b10(ES6)</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> firstName: <span class="built_in">string</span> = <span class="string">'emma'</span></span><br><span class="line"><span class="keyword">let</span> message: <span class="built_in">string</span> = <span class="string">`Hello, <span class="subst">$&#123;firstName&#125;</span>, age is <span class="subst">$&#123;age&#125;</span>`</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 以下兩個為所有類型的子類型，意思為可以被賦值給其他類型</span></span><br><span class="line"><span class="keyword">let</span> u: <span class="literal">undefined</span> = <span class="literal">undefined</span></span><br><span class="line"><span class="keyword">let</span> n: <span class="literal">null</span> = <span class="literal">null</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> num: <span class="built_in">number</span> = <span class="literal">undefined</span></span><br><span class="line"><span class="comment">// undefined 的變量可以賦值給 number</span></span><br></pre></td></tr></table></figure>
<hr>
<h4 id="any類型和聯合類型"><a class="header-anchor" href="#any類型和聯合類型">¶</a>any類型和聯合類型</h4>
<h5 id="any-允許賦值為任意類型"><a class="header-anchor" href="#any-允許賦值為任意類型">¶</a>any: 允許賦值為任意類型</h5>
<blockquote>
<p>應用狀況：不曉得原本的資料類型，例如接API傳回來的資料。應該盡量避免使用，變數的型別可以輕易被改變，另外記得返回類型一定會是any。</p>
</blockquote>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> notSure: <span class="built_in">any</span> = <span class="number">4</span></span><br><span class="line">notSure = <span class="string">'maybe it is a string'</span></span><br><span class="line">notSure = <span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 屬型和方法也可以被調用</span></span><br><span class="line">notSure.myName</span><br><span class="line">notSure.getName()</span><br></pre></td></tr></table></figure>
<h5 id="聯合類型"><a class="header-anchor" href="#聯合類型">¶</a>聯合類型</h5>
<blockquote>
<p>應用狀況：只能使用特定允許的類型</p>
</blockquote>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> numberOrString: <span class="built_in">number</span> | <span class="built_in">string</span> = <span class="number">234</span></span><br><span class="line">numberOrString = <span class="string">'abc'</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>TypeScript學習筆記</category>
      </categories>
      <tags>
        <tag>TypeScript</tag>
        <tag>學習心得</tag>
        <tag>前端</tag>
      </tags>
  </entry>
  <entry>
    <title>TypeScript 學習系列 | 函式推斷與類(class)的方法</title>
    <url>/2020/06/03/TypeScript-%E5%AD%B8%E7%BF%92%E7%B3%BB%E5%88%97-%E5%87%BD%E5%BC%8F%E6%8E%A8%E6%96%B7%E8%88%87%E9%A1%9E(class)%E7%9A%84%E6%96%B9%E6%B3%95%EF%BC%9F/</url>
    <content><![CDATA[<h3 id="Function-函式"><a class="header-anchor" href="#Function-函式">¶</a>Function 函式</h3>
<p>在參數位置定義參數類別，返回值則是在括號後方加上冒號及其型別，可選參數可被放在參數最後一個。</p>
<a id="more"></a>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函式宣告式</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">x: <span class="built_in">number</span>, y: <span class="built_in">number</span>, z?: <span class="built_in">number</span></span>): <span class="title">number</span> </span>&#123;</span><br><span class="line">	<span class="comment">// 可選參數必須被放在最後一個</span></span><br><span class="line">	<span class="keyword">if</span> (<span class="keyword">typeof</span> z === <span class="string">'number'</span>) &#123;</span><br><span class="line">		<span class="keyword">return</span> x + y + z</span><br><span class="line">	&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> x + y</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> result = add(<span class="number">2</span>, <span class="number">3</span>, <span class="number">5</span>)</span><br></pre></td></tr></table></figure>
<!--more-->
<p>也可以把參數 z 寫成這樣子：<br>
z = 10，默認 z 為 number 的寫法（ES6)</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 函式宣告式</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">x: <span class="built_in">number</span>, y: <span class="built_in">number</span>, z: <span class="built_in">number</span> = 10</span>): <span class="title">number</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (<span class="keyword">typeof</span> z === <span class="string">'number'</span>) &#123;</span><br><span class="line">		<span class="keyword">return</span> x + y + z</span><br><span class="line">	&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> x + y</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> result = add(<span class="number">2</span>, <span class="number">3</span>, <span class="number">5</span>)</span><br></pre></td></tr></table></figure>
<p>寫成函示表達式的話就是這樣子：</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">const</span> add = <span class="function"><span class="keyword">function</span> (<span class="params">x: <span class="built_in">number</span>, y: <span class="built_in">number</span>, z: <span class="built_in">number</span> = 10</span>): <span class="title">number</span> </span>&#123;</span><br><span class="line">	<span class="keyword">if</span> (<span class="keyword">typeof</span> z === <span class="string">'number'</span>) &#123;</span><br><span class="line">		<span class="keyword">return</span> x + y + z</span><br><span class="line">	&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">		<span class="keyword">return</span> x + y</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>把滑鼠移到變數 add 上看，TypeScript 會默認成函式類型，想要賦值成其他類型會報錯<br>
<img src="https://upload.cc/i1/2020/06/03/iNTOY5.png" alt="example"><br>
<img src="https://upload.cc/i1/2020/06/03/r5x6hH.png" alt="example"></p>
<p>要將變數定義為函式類別，寫法如下，注意箭頭為 TS 返回聲明函數的值的方法，不是箭頭函示的箭頭～</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">const add2: (x: number, y: number, z?: number) &#x3D;&gt; number &#x3D; add</span><br></pre></td></tr></table></figure>
<hr>
<blockquote>
<p>Tips: 直接在 node 環境上看 TS 檔案 console 結果的方法</p>
</blockquote>
<pre><code>npm install -g ts-node 
</code></pre>
<p>執行只要打這樣，xxxx 是你的檔案</p>
<pre><code>ts-node xxxx.ts
</code></pre>
<hr>
<h3 id="Class-類"><a class="header-anchor" href="#Class-類">¶</a>Class 類</h3>
<p>接下來會講到 class 父類別和子類別的方法，如果有學過 Java OOP 概念的人，在這部分應該會理解比較快，主要是其中最重要的三個觀念：封裝、繼承、多形。</p>
<p>封裝主要是不讓外部直接取得變數，但可以透過內部方法調用取得值。繼承的話，子類別可以從父類別取得其屬性和方法，也可以加上自己的屬性或方法覆寫。多形可以想像成繼承的方法，其本質不一樣，例如動物會叫，叫的聲音卻不一樣，透過多形的寫法，繼承父類別叫的方法，在子類別中 overriding(覆寫) 可以執行獲得不同的叫聲。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; 先做出一個動物的 class</span><br><span class="line"></span><br><span class="line">class Animal &#123;</span><br><span class="line">	name: string;</span><br><span class="line">		constructor(name: string) &#123;</span><br><span class="line">		this.name &#x3D; name</span><br><span class="line">	&#125;</span><br><span class="line">	run() &#123;</span><br><span class="line">		return &#96;$&#123;this.name&#125; is running&#96;</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>接著利用動物這個類別創造出物件，比如熊🐻，父類別裡面有 run 這個方法，子類別可以繼承並且使用。</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> bear = <span class="keyword">new</span> Animal(<span class="string">'Winne'</span>)</span><br><span class="line"><span class="built_in">console</span>.log(bear())</span><br><span class="line"><span class="comment">// Winne is running</span></span><br></pre></td></tr></table></figure>
<p>前面提到子類別可以自行增加方法，使用 extends 表示繼承 Animal 這個 class，下面的範例是創造出狗🐶的物件當例子，並且自行加上 bark 這個方法。</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">class</span> Dog <span class="keyword">extends</span> Animal &#123;</span><br><span class="line">	bark() &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;<span class="keyword">this</span>.name&#125;</span> is barking`</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> baobao = <span class="keyword">new</span> Dog(<span class="string">'baobao'</span>)</span><br><span class="line"><span class="built_in">console</span>.log(baobao.run()) <span class="comment">// baobao.run is running</span></span><br><span class="line"><span class="built_in">console</span>.log(baobao.bark()) <span class="comment">// baobao.run is barking</span></span><br></pre></td></tr></table></figure>
<p>前面有提到，雖然動物都會叫，可是叫聲可能會不一樣，接下來要提到多形(polymorphism)的概念和方法。在貓咪類別中利用父類別的 run 方法，加上子類別自己特別的行為。允許在程式上只要制定好一個類別，能使用來操作不同物件。</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">class</span> Cat <span class="keyword">extends</span> Animal &#123;</span><br><span class="line">	<span class="comment">// 指到父類別的變數，需要使用 super</span></span><br><span class="line">	<span class="keyword">constructor</span>(<span class="params">name</span>) &#123;</span><br><span class="line">		<span class="keyword">super</span>(name)</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// overriding 貓咪叫聲</span></span><br><span class="line">	run() &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="string">'Meow, '</span> + <span class="keyword">super</span>.run()</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> maomao = <span class="keyword">new</span> Cat(<span class="string">'maomao'</span>)</span><br><span class="line"><span class="built_in">console</span>.log(maomao.run())</span><br><span class="line"><span class="comment">//Meow, maomao is running</span></span><br></pre></td></tr></table></figure>
<h4 id="Class-修飾符"><a class="header-anchor" href="#Class-修飾符">¶</a>Class 修飾符</h4>
<blockquote>
<p>修飾符包含：public, private, protected, readonly。使用修飾符是為了對變數和方法進行權限的管理，不讓自己類別外的成員任意調用或是改變值。</p>
</blockquote>
<h5 id="1-public"><a class="header-anchor" href="#1-public">¶</a>1. public</h5>
<p>public 顧名思義就是公開，可以公開被讀取和寫入。<br>
以剛剛的例子來說，可以在 name 之前加上修飾符 private，那麼創造出來的物件就無法任意改動 name 的值。</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">class</span> Animal &#123;</span><br><span class="line">	<span class="keyword">private</span> name: <span class="built_in">string</span>;</span><br><span class="line">		<span class="keyword">constructor</span>(<span class="params">name: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">		<span class="keyword">this</span>.name = name</span><br><span class="line">	&#125;</span><br><span class="line">	run() &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;<span class="keyword">this</span>.name&#125;</span> is running`</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">snake.name = <span class="string">'Eric'</span> <span class="comment">// 會報錯</span></span><br><span class="line"><span class="built_in">console</span>.log(snake.name)</span><br></pre></td></tr></table></figure>
<h5 id="2-private-protected-readonly"><a class="header-anchor" href="#2-private-protected-readonly">¶</a>2. private / protected / readonly</h5>
<p>將 name 的權限設成 private，所以只有自身類別可以存取使用。那如果希望可以被子類別訪問呢？則可以使用 protected。 最後 readonly 則是只能取得值而無法被更改。</p>
<p><img src="https://upload.cc/i1/2020/06/04/ZmvnCQ.png" alt="example"></p>
<h5 id="3-static"><a class="header-anchor" href="#3-static">¶</a>3. static</h5>
<p>static 屬於不會被實例化的屬性，也可以稱作靜態方法，假如類的定義和實例沒有太大關係，可以寫成靜態方法。</p>
<figure class="highlight typescript"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">class</span> Animal &#123;</span><br><span class="line">	readonly name: <span class="built_in">string</span>;</span><br><span class="line">	<span class="comment">// 靜態方法</span></span><br><span class="line">	<span class="keyword">static</span> categories: <span class="built_in">string</span>[] = [<span class="string">'mammal'</span>, <span class="string">'bird'</span>]</span><br><span class="line">	<span class="keyword">static</span> isAnimal(a) &#123;</span><br><span class="line">		<span class="keyword">return</span> a <span class="keyword">instanceof</span> Animal</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">constructor</span>(<span class="params">name: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">		<span class="keyword">this</span>.name = name</span><br><span class="line">	&#125;</span><br><span class="line">	run() &#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="string">`<span class="subst">$&#123;<span class="keyword">this</span>.name&#125;</span> is running`</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(Animal.categories) <span class="comment">//[ 'mammal', 'bird' ]</span></span><br><span class="line"><span class="built_in">console</span>.log(Animal.isAnimal(snake)) <span class="comment">//true</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>TypeScript學習筆記</category>
      </categories>
      <tags>
        <tag>TypeScript</tag>
        <tag>學習心得</tag>
        <tag>前端</tag>
      </tags>
  </entry>
</search>
